# C14 — 표현 뉘앙스 비교 (클러스터 30 + 비교 UI)

규모: 중. 의존: C11 먼저면 리액션 계열 클러스터 시드 재사용. 
목표 한 줄: "비슷한 표현인데 뭐가 다른지"를 학습자가 **혼동이 생기는 바로 그 자리에서 나란히** 볼 수 있게 한다.

## 실측 근거 — "설명이 없는" 게 아니라 "안 보이는" 것 (2026-07-17)
- nuance/commonMistakes 커버리지 **~100%**(words 545·expressions 807·extendedVocab 2296 전부),
  structuredNuance 85~98%. 클러스터 상호참조도 다수(진짜↔정말, 너무↔아주, 감사합니다↔고마워요, 빨리↔일찍…).
- 그러나: ① 설명이 사전 상세(EntryDetail)의 접힌 콜아웃 안에만 있음 — 레슨·연습 중엔 안 보임.
  ② **나란히 비교하는 화면이 없음**(A항목 안에서 B를 언급할 뿐, A/B/C를 표로 대조 불가).
  문법 대조(patternContrast 30쌍)는 있는데 **표현 대조는 없음**.
  ③ 짝 항목 부재/편측: 미안해요 없음(죄송해요만), 알았어요 없음(알겠어요만), 괜찮아요↔좋아요 상호참조 없음.

## 구현

### Part A. 클러스터 데이터 `scripts/expression-clusters.json` (체크인, ~30개)
```json
{"id":"cluster-really","title":"진짜 vs 정말 vs 참","members":["진짜","정말","참"],
 "rule":"All mean 'really' — 진짜 is the casual spoken default, 정말 fits any register, 참 sounds a bit warm/old-fashioned and often opens a sentence.",
 "rows":[{"hangul":"진짜","when":"casual speech, texting","example":{"ko":"이거 진짜 맛있어요.","en":"This is really good."}}, …]}
```
- 클러스터 후보(생성 스펙에 동봉, 리뷰로 확정·증감): 진짜/정말/참 · 너무/아주/되게/정말 · 미안해요/죄송해요 ·
  알겠어요/알았어요 · 괜찮아요/좋아요/됐어요 · 감사합니다/고맙습니다/고마워요 · 같이/함께 · 빨리/일찍/어서 ·
  잘/자주/잘못 · 그리고/그래서/그런데/그러면 · 아마/혹시/어쩌면 · 조금/좀/약간 · 다/모두/전부 ·
  이제/지금/방금/아까 · 먼저/우선/일단 · 재미있어요/즐거워요/웃겨요 · 예쁘다/아름답다/귀엽다 ·
  보다/구경하다/살펴보다 · 말하다/이야기하다/얘기하다 · 만나다/보다(사람) · 알다/알아보다/알아듣다 ·
  기다리다/기대하다 · 끝나다/마치다/그만하다 · 시작하다/출발하다 · 열심히/열심히 하다 관용 ·
  그렇군요/아 그래요?/그러니까요(C11 시드와 연결) · 네/예/응 · 안녕히 가세요/안녕히 계세요 ·
  주세요/주시겠어요?/부탁드려요 · 어때요?/괜찮아요?
- 각 클러스터: rule(영어 1~2문장 — **언제 어느 것**이 기준, 사전 정의 반복 금지) + 멤버별 when/example.
  `members`는 hangul → **빌드 타임에 entryId 해석, 미해석은 빌드 에러**(vocab-packs 전례).
- **부재 짝 항목 시드 추가**: 미안해요, 알았어요, (검토 후) 어쩌면·방금 등 — expr/vocab 시드 규칙·리뷰 준수.
- 생성→교차 리뷰→적용(리뷰 중점: rule의 사실 정확성 — 과단순화로 틀린 규칙 만들지 않기. 예: 너무=부정 전용이라는
  낡은 규칙 금지, 현대 구어에서 긍정 강조 표준임).

### Part B. 비교 UI
1. **EntryDetail**: 항목이 클러스터 멤버면 접이식 섹션 **"Similar words · 뭐가 달라요?"** —
   rule 1줄 + 멤버 표(단어 | 언제 | 예문), 현재 항목 행 하이라이트, 다른 멤버는 사전 링크.
2. **레슨 단어 화면(WordsScreen)**: 단어가 클러스터 멤버면 "Example & why" 폴드 안에 한 줄 추가:
   `vs 정말 — 진짜 is the casual spoken pick.` (rule에서 파생한 초압축 1줄, 클러스터 데이터에 `hint` 필드로 저작)
   + "전체 비교 보기" → 사전 상세로 링크.
3. Dictionary 검색 결과에서 클러스터 멤버 검색 시 결과 상단에 클러스터 배지(선택 — 부담되면 v1 생략, 결정 기록).

### Part C. 검증
- lint-content 확장: 클러스터 members 실존·중복 클러스터 소속 검사(한 단어가 2개 클러스터까지 허용, 3개↑ 경고).

## 수용 기준
1. 클러스터 ≥30개, 전 멤버 entryId 해석 성공, 리뷰 통과(수정 건수 기록).
2. EntryDetail 비교 표 렌더 + WordsScreen 힌트 1줄 렌더(브라우저 확인 — 진짜/정말로 시연).
3. 부재 짝 항목(미안해요·알았어요 최소 2개) 시드 추가·사전 노출 확인.
4. 테스트 green + build + lint:content(클러스터 검사 포함). 5. 모바일에서 표 가로 스크롤 없이 읽힘.

## 하지 말 것
- 기존 nuance 텍스트 대량 재작성(클러스터는 **추가 레이어** — 기존 설명과 모순만 리뷰에서 잡기).
- 클러스터 퀴즈(v1 범위 밖 — C4 대조 뱅크 확장으로 후일). 50개 초과 확장.

## 완료 기록
✅ done 2026-07-17 — Opus 4.8 직접 구현.

### 실측이 스펙과 달랐던 점 (스펙 감사 정정)
- 스펙: "미안해요 없음(죄송해요만)" → **실측은 정반대**. 미안해요는 있고 **죄송해요가 없었다**(죄송합니다만 존재).
- 착용동사 클러스터(입다/신다/쓰다/차다) 후보는 **폐기**: 사전의 `쓰다`는 "to write / use", `차다`는 "to kick"으로
  등재돼 있어 클러스터를 만들면 사전 뜻과 모순. 같은 이유로 사람/분/명도 폐기(`분`=minute).
- 구경하다·그만하다는 사전 부재 → 해당 클러스터에서 제외.

### Part A — 클러스터 32개 (`scripts/expression-clusters.json`, 멤버 89)
- 생성→교차 리뷰→적용 3단계 준수. 생성 4에이전트(8개씩) → **다른** 에이전트 2명이 교차 리뷰(16/16씩) →
  리뷰의 `field`+`fix`를 스크립트로 기계 적용(옮겨적기 오류 방지). **수정 16건**(high 3 / medium 6 / low 7).
  리뷰가 잡은 대표 오류 — 전부 "깔끔하지만 거짓인 규칙":
  · `다`를 "부사 전용, 조사 안 붙음"이라 단언(거짓 — 이게 다예요/그게 다가 아니에요) ← 클러스터의 대들보 문장이었음
  · `잘`은 "빈도 아님" 절대화(거짓 — 감기에 잘 걸려요)
  · `되게`는 "문어에 안 씀"(과장 — 카톡·텍스트에서 표준)
  · `끝나다/마치다`가 일상어 `끝내다`를 묻고 격식체를 기본으로 가르침
  · `안녕히 가세요` 힌트가 "떠나는 사람의 인사"로 읽혀 **초보 오해를 그대로 재생산**(이 클러스터의 존재 이유를 배반)
- 스키마: `{id,title,rule,members[{hangul,entryId,when,hint,example{ko,en}}]}`. `english`는 **의도적으로 미보유** —
  뜻풀이는 사전이 소유하고 클러스터는 판단 규칙만 소유(중복·드리프트 방지). 빌드가 entryEnglish를 주입.
- 멤버는 hangul로 저작 → **빌드 타임 entryId 해석**(오타·모호어는 빌드 실패, vocab-packs 전례). 89/89 해석 성공.

### 부재 짝 항목 4건 시드 추가 (`scripts/expr-src/x-nuance-pairs.json`)
죄송해요 · 알았어요 · 좋아요 · 됐어요 (전부 고빈도 해요체). x-reactions-basic.json 스키마·깊이 그대로.
전용 리뷰어가 16건 지적 → 전부 반영. 가장 위험했던 것:
- **좋아요**: "어느 상대에게나 안전, 무례할 위험 없음"이라고 **안전 보증처럼** 서술 + 칩 `동료·상사 → 좋아요`.
  상사 제안에 맨 `좋아요`는 상대 아이디어를 평가하는 뉘앙스 → 앱이 막으려는 바로 그 실수를 가르칠 뻔.
  → `상사 → 네, 좋습니다`로 교정.
- **됐어요**: 격식 거절로 `됐습니다`를 **창작**(원어민은 "다 됐다"로 들음) → 제거하고 괜찮습니다로 정정.
  고빈도 중립 의미 `다 됐어요` 누락도 보강.
- **죄송해요**: "죄송해요와 죄송합니다는 격식만 다르다"는 과단정 완화 + 예약 누락에 **잘못 없는 손님이 사과**하는 예문 수정.

### Part B — 비교 UI
1. **EntryDetail**: 접이식 "Similar words · 뭐가 달라요?"(기본 접힘). rule 1줄 + 멤버별 언제/예문,
   현재 항목 `this one` 하이라이트, 다른 멤버는 클릭 시 그 항목으로 이동(비교 표가 새 항목 기준으로 재고정).
   **표(table) 대신 행 레이아웃** — 표는 375px에서 가로 스크롤을 강제하므로. Nuance 콜아웃 **앞**에 배치(혼동이 나는 자리).
2. **WordsScreen**: "Example & why" 폴드 안에 `vs 안녕히 가세요 — 안녕히 계세요 is your goodbye as you walk out.`
   한 줄. `clusterHint.js`로 분리해 TDD. **65과 중 37과**의 단어 화면에서 노출.
3. **레슨→사전 점프 링크는 넣지 않음(결정 기록)**: 스펙 Part B-2가 요구했으나, 레슨 중 사전으로 이탈시키면
   짧은 세션 흐름이 끊기고 사용자가 지적한 "자료집 느낌"이 되살아난다. 힌트가 그 자리에서 답을 주고,
   전체 비교는 궁금해서 사전에 갔을 때 본다. Part B-3 Dictionary 배지도 v1 생략(같은 이유 + 과설계).

### Part C — 검증
- `lint-content.mjs`에 클러스터 검사 추가: 멤버 entryId 실존 / rule·title·when·hint·example 필수 /
  최소 2멤버 / 클러스터 내 중복 / id 중복 / **한 단어 최대 2클러스터**(3개↑ 실패).
- 린트가 **실제로 잡는지 인위적 결함 7종을 주입해 증명**하고 그 증명을 `scripts/lint-clusters.test.js`로 고정.
- TDD: `expressionClusters.test.js`(5) + `clusterHint.test.js`(4) + `lint-clusters.test.js`(9).
- **68파일 / 352 테스트 green**(C15 시점 335 → +17). build + bundle guard + lint:content + integrity PASS.
- 브라우저 QA(375px): 진짜 상세에서 비교 표 렌더·기본 접힘·현재 항목 하이라이트·멤버 링크 이동·**가로 스크롤 없음**,
  신규 시드 죄송해요 사전 실존 + 클러스터 연결, 2과 레슨 단어 화면 힌트 2줄 노출(스크린샷).

### 곁가지 수정 1건
`너무`의 사전 뜻풀이가 `"too much"` 단독이라 **앱이 새로 가르치는 규칙("너무는 부정 전용이 아니다")과 정면 모순**이었다
→ `"so, really (also: too much)"`로 교정(시드 `vocab-src/24-adverbs.json`). WS2 무결성 가드가 이 변경을
"기존 항목 소실"로 정확히 차단했고, data-manifest에서 구 키를 제거해 의도된 변경임을 밝힌 뒤
`--accept-manifest-additions` → `--update-manifest`로 재등록. 항목 수 3848 유지(손실 0).

### 남긴 것
- boot gzip 1,152,852 → **1,164,632** (+11.5KB, 목표 1,200,000 이내 / 여유 약 35KB). 다음 boot 추가 시 주의.
- 리뷰어 지적 중 미반영 1건: `기대하다`가 A1–A2 앱에서 B1 태그(짝 `기다리다`는 A1). 클러스터 가치는 유효해 유지.
- 클러스터 퀴즈는 스펙대로 범위 밖(C4 대조 뱅크 확장으로 후일).
